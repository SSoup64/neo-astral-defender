[package]
name = "neo-astral-defender"
version = "0.1.0"
edition = "2024"

[dependencies]
dotenv = "0.15.0"
serenity = { version = "0.12.4", default-features = false, features = ["client", "gateway", "model", "rustls_backend"] }
tokio = { version = "1.47.1", features = ["rt-multi-thread"] }
